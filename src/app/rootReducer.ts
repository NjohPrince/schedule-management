import appointmentSlice from '../features/appointment/slice/appointmentSlice';
import getStatsSlice from '../features/appointment/slice/getStatsSlice';

// eslint-disable-next-line
export default {
    appointmentSlice,
    getStatsSlice,
};
