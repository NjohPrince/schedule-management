import React from 'react';

// stylesheet
import styles from './footer.module.css';

const FooterComponent = () => {
    return (
        <footer>
            <div className={styles.footer__component}>FooterComponent</div>
        </footer>
    );
};

export default FooterComponent;
